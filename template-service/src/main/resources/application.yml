server:
  port: 8083
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_notifications
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  redis:
    host: localhost
    port: 6379